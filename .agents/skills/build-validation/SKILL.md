---
name: build-validation
description: Use when Ronan's Flag Game changes should be verified by a production build.
---

- Confirm repo root, branch, and git status before edits.
- Run `npm run build` after code changes.
- Treat build failures as blockers until resolved or explained.
- Record the exact build result.
- Report files changed, validation run, and final git status.

