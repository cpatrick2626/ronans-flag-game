---
name: runtime-validation
description: Use when Ronan's Flag Game changes must be checked with build or runtime validation.
---

- Confirm repo root, branch, and git status before edits.
- Run the smallest relevant validation after the change.
- Prefer build or targeted runtime checks over assumptions.
- Record the exact command(s) run.
- Report pass/fail, blockers, and final git status.

