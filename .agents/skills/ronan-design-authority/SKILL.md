---
name: ronan-design-authority
description: Use when changing Ronan's Flag Game UI so the game keeps its established title, module, and reward presentation.
---

- Confirm repo root, branch, and git status before edits.
- Preserve the PLAY-gated title screen unless explicitly asked to change it.
- Keep the identity of `COLOR THE FLAG`, `Current Flag`, `Explorer Log`, and `CompletionOverlay`.
- Avoid redesigning unrelated screens.
- Report files changed, validation run, and final git status.

